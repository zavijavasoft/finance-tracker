<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mashjulal.android.financetracker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.main.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".presentation.main.MainActivity" />
        <activity
            android:screenOrientation="portrait"
            android:label="@string/title_activity_edit_operation"
            android:name=".presentation.editoperation.EditOperationActivity"
            android:parentActivityName=".presentation.main.MainActivity" />
    </application>

</manifest>